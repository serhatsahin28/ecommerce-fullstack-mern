const express = require('express');
const products = require('../../models/products');
const adminProductController=require("../../");

const router = express.Router();

router.get('/admin/productsList',);

module.exports = router;
